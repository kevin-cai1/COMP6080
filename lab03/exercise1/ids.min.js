var uuid=require("uuid");let uuids=[];var letterDict={};for(let t=0;t<30;t++){var idString=uuid.v4();for(char in idString)"-"!==idString[char]&&(letterDict[idString[char]]=(letterDict[idString[char]]||0)+1);uuids.push(idString)}var sorted=Object.keys(letterDict).sort((function(t,r){return letterDict[t]-letterDict[r]}));for(id in uuids.sort(),uuids)console.log(uuids[id]);var commonLetters="";for(letter in sorted.slice(-5))commonLetters=commonLetters+sorted[letter]+" ";console.log(commonLetters);
